{
  "name": "AmazonProject",
  "version": "1.0.0",
  "private": true,
  "scripts": {
     "start": "waitress-serve --port=$PORT app:app",
    "heroku-postbuild": "cd frontend && npm install && npm run build"
  },
  "devDependencies": {
    "concurrently": "^6.0.0"
  }
}
